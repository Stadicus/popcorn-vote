// Value substituted at build time, see vite.config.ts. In a file of its own so
// that the magic constant appears exactly once and the rest of the app uses an
// ordinary import.
export const APP_VERSION: string = __APP_VERSION__;
